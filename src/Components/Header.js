import React from "react";

const Header = () =>{
    let headerTitle = "BINT(Because it's not Twitter)";
    return(
        <div>
            <p>{headerTitle}</p>
            
        </div>
    );
};


export default Header;